import ContainerPrototype from "../components/prototypes/ContainerPrototype.tsx";
import styled from "styled-components";

export default function WTP(): JSX.Element {
	return <>Who is that Pokemon Placeholder</>;
}
